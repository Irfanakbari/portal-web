// theme/themeConfig.ts
import type { ThemeConfig } from "antd";

const theme: ThemeConfig = {
  token: {
    fontSize: 16,
    // colorPrimary: '#52c41a',
    // colorBgContainer: '#3da0e3'
  },
};

export default theme;